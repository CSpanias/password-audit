[project]
name = "password-audit"
version = "0.1.0"
description = "Password audit framework"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
password-audit = "cli.main:main"